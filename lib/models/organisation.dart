class Organisation {
  String name;
  String description;
  String organisationId;
  String location;
  List<Map<String, dynamic>> items;
  
  Organisation({this.name, this.description, this.organisationId, this.location, this.items});
}
